<UserControl
    x:Class="Virtual_Piano.Midi.Controllers.TrackChannel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    >

    <Grid
        Padding="4"
        ColumnSpacing="4"
        BorderThickness="0,1,1,0"
        BorderBrush="{ThemeResource AppBarBorderThemeBrush}"
        Background="{ThemeResource AppBarBackgroundThemeBrush}"
        >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="24"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="52"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock x:Name="LabelTextBlock" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!--Text-->
        </TextBlock>

        <TextBlock x:Name="TextBlock" Grid.Column="1" VerticalAlignment="Center" TextWrapping="WrapWholeWords">
            <!--Text-->
        </TextBlock>

        <!--<local:IndicatorGauge Grid.Column="2"/>-->
        <Button x:Name="Button" Grid.Column="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <SymbolIcon Symbol="Edit"/>
        </Button>
        
    </Grid>
</UserControl>
