<Canvas
    x:Class="Virtual_Piano.Midi.Controllers.TimeSignaturesPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="400"
    d:DesignHeight="400"
    Height="60"
    >

    <Canvas x:Name="DenominatorCanvas" Canvas.Top="45" Height="30">
        <FrameworkElement.Resources>
            <Style TargetType="Line">
                <Setter Property="StrokeThickness" Value="2"/>
                <Setter Property="Stroke" Value="Gray"/>
                <Setter Property="Y2" Value="15"/>
            </Style>
        </FrameworkElement.Resources>
    </Canvas>
    
    <Canvas x:Name="NumeratorCanvas" Canvas.Top="30" Height="30">
        <FrameworkElement.Resources>
            <Style TargetType="Line">
                <Setter Property="StrokeThickness" Value="2"/>
                <Setter Property="Stroke" Value="{ThemeResource SystemColorControlAccentBrush}"/>
                <Setter Property="Y2" Value="30"/>
            </Style>
        </FrameworkElement.Resources>
    </Canvas>

    <Line Y2="60" StrokeThickness="2" Stroke="{ThemeResource SystemColorControlAccentBrush}"/>
  
    <Line x:Name="Line" X1="400" X2="400" Y2="60" StrokeThickness="2" Stroke="{ThemeResource SystemColorControlAccentBrush}"/>

</Canvas>