<StackPanel
    x:Class="Virtual_Piano.Elements.DSTimer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Virtual_Piano.Elements"
    mc:Ignorable="d"
    d:DesignWidth="200"
    d:DesignHeight="50"
    Orientation="Horizontal"
    Spacing="4"
    >

    <FrameworkElement.Resources>
        <!--Style-->
        <Style TargetType="Polygon">
            <Setter Property="Fill" Value="SkyBlue"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="SkyBlue"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Margin" Value="2"/>
        </Style>
    </FrameworkElement.Resources>

    <local:DSNumber x:Name="MT"/>
    <local:DSNumber x:Name="M"/>
    <TextBlock>
        :
    </TextBlock>
    <local:DSNumber x:Name="ST"/>
    <local:DSNumber x:Name="S"/>
    <TextBlock>
        .
    </TextBlock>
    <local:DSNumber x:Name="MST"/>
    <local:DSNumber x:Name="MS"/>

</StackPanel>