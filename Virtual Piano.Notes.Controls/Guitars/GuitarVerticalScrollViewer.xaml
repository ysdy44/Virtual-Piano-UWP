<StackPanel
    x:Class="Virtual_Piano.Notes.Controls.GuitarScrollBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="50"
    d:DesignHeight="400"
    >

    <FrameworkElement.Resources>
        <Style TargetType="RepeatButton" BasedOn="{StaticResource TextBlockButtonStyle}">
            <Setter Property="Width" Value="60"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>
    </FrameworkElement.Resources>

    <RepeatButton x:Name="PreviousButton">
        <SymbolIcon Symbol="Previous"/>
    </RepeatButton>
    <AppBarSeparator/>
    <RepeatButton x:Name="NextButton" Grid.Row="2">
        <SymbolIcon Symbol="Next"/>
    </RepeatButton>

</StackPanel>